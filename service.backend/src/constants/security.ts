/**
 * Shared security-related constants (ADS-524).
 */

/**
 * HSTS `max-age` directive value, expressed in seconds.
 * One year is the value recommended by hstspreload.org for sites that opt
 * in to the preload list.
 */
export const HSTS_MAX_AGE_ONE_YEAR_SECONDS = 31_536_000;
