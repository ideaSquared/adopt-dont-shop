# @adopt-dont-shop/lib.matching

## Purpose

Shared TypeScript types for the pet-adopter matching feature. A zero-runtime,
types-only package that mirrors the surface of
[`services/matching`](../../services/matching/README.md) (the gRPC
`MatchingService` and its read-model handlers) so backend and frontend speak the
same shapes across the API boundary (top-picks response, discovery pet card,
match profile read/write).

## Location in the architecture

See [`docs/README.md`](../../docs/README.md#libraries) for where the shared
libraries sit. Mirrors `services/matching` (the source of truth) and is consumed
by [`lib.discovery`](../lib.discovery/README.md), which surfaces match picks in
the swipe UI.

## Scripts

```bash
pnpm dev          # build --watch
pnpm build        # tsc → dist/
pnpm test         # Vitest (run mode)
pnpm lint         # ESLint
pnpm type-check   # TypeScript type-check
```

## Public API / exports

The canonical list lives in [`src/index.ts`](src/index.ts):

- `REASON_CHIP_KINDS` (const tuple: `pref_match`, `lifestyle`, `distance`,
  `similar_to_liked`, `fresh`), `ReasonChipKind`, `isReasonChipKind(value)`
  (type guard), `ReasonChip`.
- `AdopterLifestyle`, `AdopterMatchProfile` (preferred types/sizes/ages/energy,
  lifestyle, distance, special-needs + notification opt-in, allergies).
- `MatchTopPick` — the top-picks endpoint shape.

## Environment variables consumed

None — types only.

## Testing notes

Vitest — the `isReasonChipKind` type guard and the const-tuple shapes are tested
directly. See [`docs/frontend/testing.md`](../../docs/testing.md) for anything
not library-specific.

## Ownership

See [`.github/CODEOWNERS`](../../.github/CODEOWNERS) for the current owner of
`/packages/`.

<!-- CONSUMERS:START (auto-generated by scripts/generate-dependency-docs.mjs — do not edit by hand) -->
## Consumers

1 workspace package(s) depend on this library. See [lib.matching-consumers.md](../../docs/libraries/lib.matching-consumers.md) for the auto-generated list — check it before making a breaking change.
<!-- CONSUMERS:END -->
