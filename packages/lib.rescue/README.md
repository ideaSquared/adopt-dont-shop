# @adopt-dont-shop/lib.rescue

## Purpose

Rescue-organisation data client for the frontend apps — profiles, pet listings,
adoption policies, and rescue search.

## Location in the architecture

See [`docs/README.md`](../../docs/README.md#libraries) for where the shared
libraries sit. Reaches the gateway's `/api/v1/rescues/*` surface over
[`service.rescue`](../../services/rescue/README.md).

## Scripts

```bash
pnpm dev          # tsc --watch
pnpm build        # tsc build
pnpm test         # Vitest (run mode)
pnpm lint         # ESLint
pnpm type-check   # TypeScript type-check
```

## Public API / exports

The canonical list lives in [`src/index.ts`](src/index.ts):

- `RescueService` (class) + `rescueService` (singleton): `getRescue`,
  `searchRescues`, `getAllRescues`, `getFeaturedRescues`, `getPetsByRescue`,
  `getAdoptionPolicies` / `updateAdoptionPolicies`.
- Types: `Rescue`, `RescueAPIResponse`, `RescueSearchFilters`, `RescueStatus`,
  `RescueType`, `RescueLocation`, `Pet`, `PaginatedResponse`,
  `RescueServiceConfig`, plus the re-exported constants.

## Environment variables consumed

None directly — reaches the backend through the shared `lib.api` client. See
[`docs/env-reference.md`](../../docs/env-reference.md) for the full list.

## Testing notes

Vitest — the service methods are tested against a mocked API (search filters,
pagination, policy read/update). See
[`docs/frontend/testing.md`](../../docs/testing.md) for anything not
library-specific.

## Ownership

See [`.github/CODEOWNERS`](../../.github/CODEOWNERS) for the current owner of
`/packages/`.

<!-- CONSUMERS:START (auto-generated by scripts/generate-dependency-docs.mjs — do not edit by hand) -->
## Consumers

3 workspace package(s) depend on this library. See [lib.rescue-consumers.md](../../docs/libraries/lib.rescue-consumers.md) for the auto-generated list — check it before making a breaking change.
<!-- CONSUMERS:END -->
