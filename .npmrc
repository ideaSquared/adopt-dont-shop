# pnpm configuration — keep the strict, symlinked node_modules layout so
# phantom dependencies surface as errors rather than working by accident.
# Add `public-hoist-pattern[]` / `shamefully-hoist` entries only if a specific
# dependency genuinely needs a flat layout.
auto-install-peers=true
