# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*.env
*.env.local
*.env.staging
*.env.production


### react ###
.DS_*
*.log
logs
**/*.backup.*
**/*.back.*

node_modules
bower_components

*.sublime*

psd
thumb
sketch

_archive/
uploads

### Turbo ###
.turbo

### Per-package lockfiles ###
# Per-package lockfiles aren't authoritative in pnpm workspaces; only the
# root pnpm-lock.yaml is. Delete any that get accidentally created (including
# stray npm/yarn lockfiles from running the wrong package manager).
/apps/*/package-lock.json
/packages/*/package-lock.json
/services/*/package-lock.json
/apps/*/pnpm-lock.yaml
/packages/*/pnpm-lock.yaml
/services/*/pnpm-lock.yaml

### Docker ###
docker-compose.override.yml
# Materialized Docker secret files (written by deploy workflow, never committed)
# — the README + the directory-local .gitignore are kept so the directory
# exists in the repo and explains the layout to operators.
secrets/*
!secrets/README.md
!secrets/.gitignore

### Development ###
.env.development
.env.development.local

### Tests ###
*/coverage
**/coverage/

### Build artifacts ###
*.tsbuildinfo
*.timestamp-*.mjs
.claude/worktrees/

## Storybook static
packages/lib.components/storybook-static/

### E2E (Playwright)
e2e/test-results/
e2e/playwright-report/
e2e/blob-report/
e2e/.auth/
e2e/playwright/.cache/

# Husky pre-push opt-in marker (ADS-732)
.husky/.prepush-enabled

.pnpm-store/