# =============================================================================
# Adopt Don't Shop - Root Environment Configuration
# =============================================================================
# This file contains SHARED configuration for all frontend applications
# App-specific overrides should go in app.*/. env files
# =============================================================================

# -----------------------------------------------------------------------------
# Environment
# -----------------------------------------------------------------------------
NODE_ENV=development

# -----------------------------------------------------------------------------
# Frontend API Configuration (Shared across all apps)
# -----------------------------------------------------------------------------
# These URLs are used by ALL frontend apps (admin, client, rescue)
# Change once, applies everywhere

# Development (direct backend access)
VITE_API_BASE_URL=http://localhost:5000
VITE_WS_BASE_URL=ws://localhost:5000

# Alternative: Nginx-proxied (uncomment to use subdomain routing)
# VITE_API_BASE_URL=http://api.localhost
# VITE_WS_BASE_URL=ws://api.localhost

# Docker Compose (when running in containers)
# VITE_API_BASE_URL=http://service-backend:5000
# VITE_WS_BASE_URL=ws://service-backend:5000

# -----------------------------------------------------------------------------
# Shared Feature Flags (All Apps)
# -----------------------------------------------------------------------------
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_CHAT=true
VITE_ENABLE_NOTIFICATIONS=true
VITE_ENABLE_DEV_TOOLS=true
VITE_ENABLE_DEBUG_LOGGING=true

# -----------------------------------------------------------------------------
# Third-Party Services (Shared)
# -----------------------------------------------------------------------------
# Analytics
VITE_GOOGLE_ANALYTICS_ID=your-ga-tracking-id

# Error Tracking
VITE_SENTRY_DSN=your-sentry-dsn-url

# Maps
VITE_MAPBOX_API_KEY=your-mapbox-api-key

# Social Login (if implemented)
VITE_GOOGLE_CLIENT_ID=your-google-oauth-client-id
VITE_FACEBOOK_APP_ID=your-facebook-app-id

# -----------------------------------------------------------------------------
# Performance Configuration (Shared)
# -----------------------------------------------------------------------------
VITE_API_TIMEOUT=30000
VITE_MAX_FILE_SIZE=5242880
VITE_ALLOWED_FILE_TYPES=image/jpeg,image/png,image/webp

# -----------------------------------------------------------------------------
# Backend/Database Configuration
# -----------------------------------------------------------------------------
# These are used by docker-compose and backend service

# Database
DEV_DB_NAME=adoptdontshop_development
TEST_DB_NAME=adoptdontshop_test
PROD_DB_NAME=adoptdontshop_production

# CORS Configuration (all origins that need API access)
CORS_ORIGIN=http://localhost:3000,http://localhost:3001,http://localhost:3002,http://localhost,http://admin.localhost,http://rescue.localhost,http://api.localhost,http://localhost:5000

# -----------------------------------------------------------------------------
# Notes
# -----------------------------------------------------------------------------
# - Frontend apps automatically inherit these variables
# - Apps can override specific variables in their own .env files
# - Backend service uses service.backend/.env (separate concerns)
# - Never commit secrets to version control
# =============================================================================
